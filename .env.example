# NTN Stack 環境變數配置範例
# 複製此文件為 .env 並修改相應的值

# === 網路配置 ===
# 外部 IP 地址 (用於 CORS 和監控)
# 生產環境：設定為實際的外部 IP 地址
# 開發環境：可以使用 127.0.0.1 或 localhost
EXTERNAL_IP=YOUR_EXTERNAL_IP_HERE

# === 常用配置範例 ===
# 生產環境範例：
# EXTERNAL_IP=203.0.113.100

# 開發環境範例：
# EXTERNAL_IP=127.0.0.1

# 內網環境範例：
# EXTERNAL_IP=192.168.1.100

ENVIRONMENT=development
MONGO_HOST=localhost
REDIS_HOST=localhost