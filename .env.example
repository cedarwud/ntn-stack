# NTN Stack 環境變數配置範例
# 複製此文件為 .env 並修改相應的值

# === 環境配置 ===
ENVIRONMENT=production
LOG_LEVEL=INFO

# === API 配置 ===
API_HOST=0.0.0.0
API_PORT=8080
API_WORKERS=2
API_TIMEOUT=30

# === 數據庫配置 ===
# PostgreSQL 配置
POSTGRES_DB=netstack_db
POSTGRES_USER=netstack_user
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_HOST=netstack-postgres
POSTGRES_PORT=5432

# MongoDB 配置
MONGO_HOST=mongo
MONGO_PORT=27017
MONGO_DB=open5gs

# Redis 配置
REDIS_HOST=redis
REDIS_PORT=6379

# === 衛星數據配置 ===
SATELLITE_DATA_MODE=instant_load
PRECOMPUTE_ON_STARTUP=true
ORBIT_CACHE_PRELOAD=true

# === 安全配置 ===
# 重要：請更改為您自己的密鑰
SECRET_KEY=change_this_to_your_own_secret_key

# === 性能配置 ===
MAX_REQUEST_SIZE=10485760
WORKER_CONNECTIONS=1000

# === 網路配置 ===
# 外部 IP 地址 (用於 CORS 和監控)
# 生產環境：設定為實際的外部 IP 地址
# 開發環境：可以使用 127.0.0.1 或 localhost
# 範例：
# EXTERNAL_IP=203.0.113.100  # 生產環境
# EXTERNAL_IP=127.0.0.1       # 開發環境
# EXTERNAL_IP=192.168.1.100   # 內網環境
EXTERNAL_IP=127.0.0.1