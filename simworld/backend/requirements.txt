fastapi
uvicorn[standard]
matplotlib
Pillow
sionna
sionna-rt
trimesh
pyrender>=0.1.45  # 確保使用較新版本，支持多種渲染後端
pyglet>=2.0.0  # 為 pyrender 提供渲染支持
PyOpenGL>=3.1.0  # OpenGL 支持
PyOpenGL_accelerate  # 可選但建議，提高 OpenGL 性能
python-multipart  # 支援表單數據和文件上傳
skyfield>=1.45  # 軌道計算和天體坐標轉換
httpx # 用於非同步 HTTP 請求
redis # 用於 Redis 客戶端
aiohttp # 用於非同步 HTTP 客戶端
psutil  # 系統和進程監控

# --- 日誌和配置 ---
structlog>=23.2.0  # 結構化日誌
python-json-logger>=2.0.7  # JSON 日誌格式
pyyaml>=6.0.1  # YAML 配置支援

# --- 數據處理 ---
numpy>=1.24.0  # 數值計算
aiofiles>=23.2.0  # 異步檔案操作

# --- 科學計算和測試依賴 ---
pandas>=2.0.0  # 數據分析和處理 (用於衛星數據處理)
scipy>=1.10.0  # 科學計算和統計 (用於軌道計算)
jinja2>=3.1.0  # 模板引擎 (報告生成)
sgp4>=2.21  # 衛星軌道計算 (SGP4/SDP4 模型) - 核心需求

# --- 移除不必要的 ML 和 PostgreSQL 依賴 ---
# 已移除：tensorflow, keras, scikit-learn, seaborn (非衛星換手核心研究)
# 已移除：sqlmodel, sqlalchemy, psycopg, asyncpg, geoalchemy2 (使用 NetStack 的 PostgreSQL)

# --- MongoDB 相關套件 (新增) ---
motor>=3.3.0        # MongoDB 的異步驅動程式

# --- 測試相關套件 ---
pytest>=7.4.0       # 測試框架
pytest-cov>=4.1.0   # 測試覆蓋率分析
pytest-asyncio>=0.21.0  # 非同步測試支援